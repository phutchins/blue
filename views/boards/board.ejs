<% boards.forEach( function ( board ) { %>
  <% board.columns.forEach( function ( column ) { %>
    <div class="col-sm-6">
      <div class="well">
        <div class="column">
          <div class="portlet">
            <div class="portlet-header"><%= column %></div>
            <div class="portlet-content">cards here</div>
          </div>
        </div>
      </div>
    </div>
  <% }); %>
<% }); %>
